---
title: CUDA 学习记录
date: 2026-05-29
tags:
  - 宅宅开发
  - CUDA
summary: 从 Thread / Block / Warp / SP 的并行模型，到一维、二维全局坐标计算的入门笔记
showTags: true
toc: true
readTime: true
math: false
---

## CUDA 并行实现

### Thread —— 最小逻辑并行单元

CUDA 里的 **Thread** 可以先理解成最小的并行单元

比如这个 kernel：

```cpp
__global__ void add_one(float* x, float* y, int n) {
    int i = threadIdx.x;

    if (i < n) {
        y[i] = x[i] + 1.0f;
    }
}
```

如果你启动 10 个 thread，那么每个 thread 都会执行这一段代码。

区别在于：

```cpp
threadIdx.x
```

每个 thread 的值不同。

例如：

```text
thread 0: threadIdx.x = 0
thread 1: threadIdx.x = 1
thread 2: threadIdx.x = 2
...
thread 9: threadIdx.x = 9
```

所以虽然所有 thread 执行同一份代码，但它们处理的数据不同。

这就是 CUDA 的基本思想。



### 为什么还需要 Block

如果只有 Thread，会有一个问题：

```text
一次 kernel 可能要启动几百万、几千万个 thread。
```

CUDA 不会把所有 thread 扔成一大坨，而是把它们分组，这个组就叫 **Block**。

可以理解成：

```text
Thread：单个学生
Block：一个班级
Grid：整个年级
```

比如我们启动：

```cpp
kernel<<<3, 4>>>(...);
```

意思是：

```text
3 个 Block
每个 Block 里有 4 个 Thread
```

所以总 thread 数是：

```text
3 × 4 = 12 个 thread
```

`threadIdx.x` 只是在 **当前 Block 内部编号**。

所以 Block 0 里面有一个 `threadIdx.x = 0`

Block 1 里面也有一个 `threadIdx.x = 0`

全局编号：

```cpp
int i = blockIdx.x * blockDim.x + threadIdx.x;
```

其中：

```text
blockIdx.x：当前是第几个 Block
blockDim.x：每个 Block 有多少个 Thread
threadIdx.x：当前 Thread 在 Block 内部的编号
```

假设：

```cpp
kernel<<<3, 4>>>(...);
```

那么：

```text
gridDim.x = 3
blockDim.x = 4
```



### SM 是什么

Streaming Multiprocessor，流式多处理器，对应逻辑上的 Block。

刚才讲的是 CUDA 的软件抽象，现在看硬件真实执行。

假设 GPU 有 2 个 SM

启动：

```cpp
kernel<<<3, 4>>>(...);
```

也就是 3 个 Block：

```text
Block 0
Block 1
Block 2
```

GPU 可能这样调度：

```text
SM 0 执行 Block 0
SM 1 执行 Block 1
Block 2 先等着
```

等某个 SM 空出来后：

```text
SM 0 执行完 Block 0
SM 0 再执行 Block 2
```

你不需要写：

```text
Block 0 去 SM 0
Block 1 去 SM 1
Block 2 去 SM 0
```

这是硬件调度器自动做的。

这个地方就要针对硬件级别进行优化。



### Warp 是什么

Warp 是 GPU 硬件调度和执行的最小物理单元，通常包含 32 个 Thread。

也就是说，你写的是 Thread，但 GPU 不会真的一个 thread 一个 thread 调度，它会把 thread 打包成 Warp。

比如你写：

```cpp
kernel<<<1, 256>>>(...);
```

意思是：

```text
1 个 Block
256 个 Thread
```

硬件会把这 256 个 Thread 分成：

```text
256 / 32 = 8 个 Warp
```

也就是：

```text
Warp 0: thread 0 ~ 31
Warp 1: thread 32 ~ 63
Warp 2: thread 64 ~ 95
...
Warp 7: thread 224 ~ 255
```

你不用手动创建 Warp。

Warp 是硬件自动形成的。

- Thread 是你写代码时看到的逻辑单位
- Warp 是硬件真正调度执行的单位

最好尽量让 CUDA 线程组织"契合 Warp" —— 32 的倍数，假如 33：

```text
Thread 0  ~ 31  → Warp 0，满的
Thread 32       → Warp 1，只有 1 个有效线程
```



### SP 又是什么

SP 是真正进行加减乘除的计算执行单元。

GPU 内部会把 Warp 的指令发给执行单元去算。

所以关系是：

```text
你控制：Grid / Block / Thread

硬件控制：Block 放到哪个 SM，Thread 怎么组成 Warp，Warp 怎么用 SP 执行
```



大致映射关系是：

```text
一个 Kernel 启动一个 Grid
一个 Grid 包含很多 Block
一个 Block 会被分配到某个 SM 上执行
一个 Block 里的 Thread 会被硬件切成多个 Warp
Warp 里的 Thread 最终在 SM 的执行单元上运行
```

最重要的是：

```text
Block → SM
Thread → Warp
```

这不是手动指定的，是硬件自动安排的。



## 全局坐标计算

### 一维坐标计算

![](https://dlog.com.cn/_astro/002.BbsP-Y1j_2sV3tM.webp)

一维数组 kernel，基本都是这个模板：

```cpp
__global__ void kernel(float* x, float* y, int N) {
    int i = blockIdx.x * blockDim.x + threadIdx.x;

    // 一定要写边界，可能会出现空余的线程 （比如 3x4 > 10）
    if (i < N) {
        y[i] = ...;
    }
}
```

主机端启动：

```cpp
int blockSize = 256;
int gridSize = (N + blockSize - 1) / blockSize; // 向上取整，覆盖所有的数据

kernel<<<gridSize, blockSize>>>(x, y, N);
```



### 二维坐标计算

![](https://dlog.com.cn/_astro/003.DbAsiMCW_1mrdxw.webp)

```cpp
// 列方向，当前块之前的数量 * 每块宽度 + 当前块内的线程偏移
int col = blockIdx.x * blockDim.x + threadIdx.x;
// 行方向，当前块之前的数量 * 每块高度 + 当前块内的线程偏移
int row = blockIdx.y * blockDim.y + threadIdx.y;
```

内存本质是一条线，不是二维格子。

```cpp
// 我们只需要利用行优先规则转换
if (row < height && col < width) {
    int global_idx = row * width + col;            // 转为一维线性地址
}
```
